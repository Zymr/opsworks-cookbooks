Chef::Log.info(" === After checkout hook for #{release_path}")
include_recipe 'deploy'

node[:deploy].each do |application, deploy|

    file "#{release_path}/canvas.presentation/app/index.html" do
      owner 'root'
      group 'root'
      mode 0644
      content ::File.open("#{release_path}/canvas.presentation/app/index.html.testkanvz").read
      action :create
  end

    file "#{release_path}/canvas.presentation/app/config.js" do
      owner 'root'
      group 'root'
      mode 0644
      content ::File.open("#{release_path}/canvas.presentation/app/config.js.testkanvz").read
      action :create
  end
end
